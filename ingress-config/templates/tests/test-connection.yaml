apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ingress-config.fullname" . }}-test-connection"
  labels:
    {{- include "ingress-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ingress-config.fullname" . }}']
  restartPolicy: Never
