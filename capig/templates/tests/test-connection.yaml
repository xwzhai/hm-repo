apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "capig.fullname" . }}-test-connection"
  labels:
    {{- include "capig.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "capig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
